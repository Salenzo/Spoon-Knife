name: ＡＢＣＤ
on:
  workflow_dispatch:
jobs:
  wow:
    runs-on: windows-latest
    steps:
      - name: CO
        uses: actions/checkout@v2
        with:
          ref: change-the-title

      - uses: GuillaumeFalourd/setup-windows10-sdk-action@v1.7
        name: W
        with:
          sdk-version: 22621

      - name: CO₂
        shell: bash
        run: |
          export SKIAVERSION="$(curl https://api.github.com/repos/aseprite/skia/releases/latest | jq -r .tag_name)"
          export VERSION="$(curl https://api.github.com/repos/aseprite/aseprite/releases/latest | jq -r .tag_name)"
          echo $SKIAVERSION, $VERSION; mkdir a c; cd a
          curl -LO "https://github.com/aseprite/aseprite/releases/download/$VERSION/Aseprite-$VERSION-Source.zip"
          ls -l; unzip Aseprite-$VERSION-Source.zip
          cd ../c
          curl -OL "https://github.com/aseprite/skia/releases/download/$SKIAVERSION/Skia-Windows-Release-x86.zip"
          ls -l; unzip Skia-Windows-Release-x86.zip
          cd ../b
          cmd.exe /c tab.bat
          cd ..
          mv d.7z docs
          git add docs/d.7z
          if git -c user.email=alice@example.com -c user.name=Alice commit --allow-empty-message -m ""
          then
            git push origin change-the-title
          fi
